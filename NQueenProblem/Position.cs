namespace NQueenProblem
{
    /// <summary>
    /// This is a simple position class which is used for
    /// any position represented on a grid (grids implemented as stacks)
    /// It is used both by Queens to represent their positions as well
    /// as to store their attack positions.
    /// </summary>
    class Position
    {
        public int x;
        public int y;
    }
}